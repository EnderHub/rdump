# Test Cases

The full set of rdump RQL examples now lives in `tests/data/doc_queries.txt` and is enforced by `tests/docs_test_cases.rs`.

This file stays lightweight so we can tell when new queries need coverage—add new examples to the test data file and the automated test will keep us honest.
